//
// Created by william on 2020/12/25.
//


// const ——定义类型为常量。
// volatile ——定义类型为易变。
// mutable - 容许在即便包含它的对象被声明为 const 时仍可修改声明为 mutable 的类成员。可出现于非引用非 const 类型的非静态数据成员的声明中：
// 若 object 是非位域右值或函数左值，且其类型是 T 或派生于 T，拥有相等或更少 cv 限定，则绑定引用到初始化器表达式的值或其基类子对象（经实质化临时量之后，若需要） (C++17 起)。



